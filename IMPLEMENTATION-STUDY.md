# دراسة تنفيذ بوت أصيل الشخصي والرقمي

## الخلاصة التنفيذية

البنية المناسبة هي خدمة Node.js على Render، وWhatsApp Cloud API كمصدر أحداث، وOpenAI كمحرك رد، وSupabase كذاكرة وقاعدة معرفة ولوحة إعدادات. تم تجهيز مسارات Meta الحالية والبديلة، وقاعدة بيانات موسعة، ولوحة تدريب محمية، وحفظ دائم لإعدادات التدريب عند تفعيل Supabase.

لا ينبغي أن يغيّر البوت نفسه أو صلاحياته أو قواعده تلقائيًا. الصيغة الآمنة هي أن يقترح تحسينات في جدول مراجعة، ثم يعتمدها المدير من لوحة الإدارة. أما تحديث التعليمات وقاعدة المعرفة من لوحة المدير فيُحفظ كإصدار قابل للتتبع.

## التحقق من Meta

توثيق Meta يوضح أن Webhook يستقبل JSON من خوادم Meta، وأن حقل `messages` هو الحقل المطلوب لرسائل WhatsApp. كما توضح Meta أن فشل الاستجابة أو أي HTTP غير 200 يؤدي إلى إعادة المحاولة لمدة قد تصل إلى سبعة أيام، وأن التكرار ممكن؛ لذلك يتضمن الخادم استجابة سريعة، ومعرّف رسالة لمنع التكرار، والتحقق الاختياري من توقيع `X-Hub-Signature-256`.[1]

المسار الأساسي:

```text
GET  /webhook
POST /webhook
```

والمسار البديل:

```text
GET  /api/webhook/meta
POST /api/webhook/meta
```

يجب استخدام مسار واحد فقط في إعداد Meta. المسار الموصى به حاليًا هو `/webhook`.

## OpenAI

توثيق OpenAI يوصي بحفظ المفتاح في متغيرات البيئة أو مدير أسرار وعدم وضعه في كود الواجهة. كما يوصي بمراقبة معرّفات الطلبات، حدود المعدل، الإنفاق، وتدوير المفاتيح.[2] يستخدم المشروع متغيرات منفصلة:

```text
AI_PROVIDER=openai
OPENAI_API_KEY=...
AI_MODEL=gpt-4o-mini
```

ولا يمكن تأكيد اتصال OpenAI فعليًا قبل إضافة المفتاح في Render وتشغيل اختبار إداري ناجح؛ ففحص الكود وحده لا يثبت صلاحية المفتاح أو الرصيد أو حدود المشروع.

## الدراسة الفعلية لزمن الاستجابة

تم قياس رابط Render الحالي قبل نشر هذه النسخة الجديدة، وكانت النتائج التالية لمسار `/health`:

| المحاولة | HTTP | زمن أول بايت | الزمن الكلي |
|---:|---:|---:|---:|
| 1 | 200 | 24.57 ثانية | 24.57 ثانية |
| 2 | 200 | 2.44 ثانية | 2.44 ثانية |
| 3 | 200 | 5.48 ثانية | 5.48 ثانية |
| 4 | 200 | 2.65 ثانية | 2.89 ثانية |
| 5 | 200 | 2.24 ثانية | 2.24 ثانية |

الاستنتاج: النسخة الحالية تتأثر ببدء التشغيل البارد على الخطة المجانية. أول طلب كان بطيئًا، بينما الطلبات التالية أسرع ولكنها ليست زمن رد OpenAI الكامل؛ لأنها تقيس Render فقط. القياس الصحيح بعد النشر يقسم الزمن إلى: استقبال Meta، استرجاع الذاكرة، استدعاء OpenAI، وإرسال رسالة WhatsApp.

لتقليل زمن الرد، يجب استخدام رد ثابت سريع عند الحاجة، تقليل طول الرد، تقليل عدد الاستدعاءات، تخزين قاعدة المعرفة مؤقتًا، وعدم إجراء بحث متجهي متعدد المراحل لكل سؤال. تتفق هذه الإجراءات مع إرشادات OpenAI التي تركز على تقليل التوكنات، تقليل عدد الطلبات، والتوازي عندما لا تكون الخطوات متسلسلة.[3]

## قاعدة البيانات الجديدة

يتضمن `supabase-schema.sql` الجداول التالية:

- `users`: هوية العميل والمنصة وحالة التحويل لموظف.
- `chat_history`: سجل الرسائل ومزود النموذج وزمن الرد ومعرّف الرسالة.
- `knowledge_base`: المعرفة المعتمدة والمتجهات والإصدارات.
- `extracted_facts`: اقتراحات الحقائق، وتبدأ غير معتمدة.
- `bot_settings`: التعليمات وقاعدة المعرفة والإعدادات.
- `bot_commands`: أوامر المدرب القابلة للتفعيل.
- `training_revisions`: سجل إصدارات التدريب.
- `webhook_events`: سجل الأحداث ومنع التكرار وتتبع الأخطاء.
- `security_events`: سجل الأحداث الأمنية دون تخزين أسرار.

اللوحة تحفظ التعليمات وقاعدة المعرفة في `bot_settings` وتسجل كل تغيير في `training_revisions`. إذا لم تُضف بيانات Supabase، يعمل التدريب مؤقتًا في ذاكرة الخدمة فقط.

## لوحة الإدارة والتدريب

المسار:

```text
/admin
```

وتحتاج إلى:

```text
ADMIN_PASSWORD
ADMIN_SESSION_SECRET
```

تقدم اللوحة محادثة اختبار، وتعديل التعليمات، وتعديل قاعدة المعرفة. التعديلات لا تمنح البوت صلاحيات تنفيذية بحد ذاتها؛ هي فقط تغير سياق الرد بعد اعتماد المدير.

## التطوير الذاتي الآمن

التطوير الذاتي المقترح لا يعني أن البوت يغيّر الكود أو يرفع نسخة جديدة أو يعدل مفاتيح Meta تلقائيًا. بدلاً من ذلك:

1. يحلل المحادثات ويستخرج اقتراحات.
2. يحفظها في `extracted_facts` مع `approved=false`.
3. يعرضها في لوحة الإدارة.
4. يعتمد المدير الاقتراح أو يرفضه.
5. بعد الاعتماد فقط تدخل المعلومة قاعدة المعرفة.

هذا يمنع أن يتحول خطأ واحد في محادثة إلى معلومة رسمية أو سعر غير صحيح.

## الحماية

الحماية الأساسية في النسخة تشمل عدم كشف المفاتيح في الواجهة، جلسة إدارة منفصلة، منع تكرار الرسائل، حد أقصى لحجم JSON، والتحقق الاختياري من توقيع Meta. للإنتاج يجب إضافة معدل طلبات لكل رقم، قفل مؤقت بعد محاولات دخول فاشلة، تدوير كلمة مرور الإدارة، وتسجيل أمني في Supabase.

يجب أيضًا تفعيل حدود الإنفاق ومراقبة استخدام OpenAI، مع فصل بيئة الاختبار عن الإنتاج. لا يتم تخزين كلمات المرور أو رموز الوصول داخل سجل المحادثة.

## ما يلزم لإثبات الجاهزية

بعد رفع النسخة إلى Render وإضافة المتغيرات، يجب اجتياز الاختبارات التالية:

1. `GET /health` يعرض `whatsapp=true` و`ai=true`.
2. تحقق Meta يعيد قيمة `hub.challenge`.
3. اختبار محادثة `/admin` يعيد ردًا حقيقيًا من OpenAI.
4. إرسال رسالة WhatsApp ينتج حدثًا في سجلات Render.
5. يعود الرد إلى العميل داخل نافذة خدمة WhatsApp البالغة 24 ساعة. خارج هذه النافذة يلزم قالب رسالة معتمد من Meta.[4]
6. حفظ تعديل تدريبي يظهر في `bot_settings` و`training_revisions`.

## الملفات المنفذة

- `index.js`: الخادم والمسارات والتكاملات.
- `supabase-schema.sql`: قاعدة البيانات الجديدة.
- `README.md`: تعليمات النشر.
- `.env.example`: المتغيرات السرية المطلوبة.

## References

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/overview "WhatsApp webhooks - Meta for Developers"
[2]: https://developers.openai.com/api/reference/overview/ "OpenAI API Overview"
[3]: https://developers.openai.com/api/docs/guides/latency-optimization "OpenAI Latency optimization"
[4]: https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/send-messages "WhatsApp service messages - Meta for Developers"
