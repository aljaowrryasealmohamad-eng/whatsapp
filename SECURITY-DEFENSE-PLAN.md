# خطة الدفاع الذاتي الآمن لبوت أصيل

## المبدأ

البوت لا يستطيع ضمان التنبؤ بكل هجوم قبل وقوعه، ولا ينبغي أن يعد بذلك. التصميم الصحيح هو دفاع طبقي مستمر: تقليل السطح المكشوف، كشف الشذوذ، احتواء الهجوم، استعادة الخدمة، ثم تحسين القواعد بعد مراجعة بشرية.

## الطبقات المنفذة

تتضمن النسخة الحالية حماية توقيع Meta الاختيارية، منع تكرار أحداث WhatsApp، حدًا لحجم الطلبات، تحديد محاولات تسجيل دخول لوحة الإدارة، جلسات إدارة مؤقتة، رؤوس أمان، منع تضمين اللوحة، منع حقن HTML في المحادثة، وإبقاء الأسرار خارج المتصفح.

## كشف الشذوذ

يُسجل النظام أحداث الويبهوك والأخطاء والأحداث الأمنية في Supabase عند تفعيله. يمكن إنشاء تنبيهات عند ارتفاع محاولات الدخول، تكرار الرسائل، زيادة أخطاء Meta، أو ارتفاع زمن استجابة الذكاء الاصطناعي. هذا كشف مبكر وليس تنبؤًا يقينيًا.

## الاستجابة الآلية الآمنة

الإجراءات التي يمكن تنفيذها تلقائيًا دون موافقة هي إجراءات قابلة للعكس: إيقاف جلسة إدارة، تخفيض معدل الطلبات، منع رقم أو مصدر مؤقتًا، تحويل المحادثة إلى موظف، وتعطيل أمر بوت محدد. لا يُسمح للنموذج بتشغيل Shell أو تعديل الكود أو الوصول إلى مفاتيح API.

الإجراءات عالية الأثر تحتاج موافقة المدير: تدوير مفتاح Meta أو OpenAI، حذف بيانات، تغيير Webhook، تغيير صلاحيات Supabase، أو نشر نسخة جديدة.

## النقل والتشفير

تستخدم الاتصالات الخارجية HTTPS إلى Render وMeta وOpenAI وSupabase. لا يوجد معنى آمن لتدمير مفاتيح API بعد كل نقل؛ ذلك سيقطع الخدمة ولا يضمن إبطال نسخة مسربة. البديل الصحيح هو استخدام أسرار قصيرة العمر متى توفرت، تدوير المفتاح عند الاشتباه، إبطال المفتاح القديم بعد اختبار الجديد، وعدم تسجيله أو إرساله للواجهة. يُراجع هذا الإجراء وفق دورة إنشاء السر وتدويره وإبطاله وانتهائه.

## التطوير الذاتي المحكوم

يقترح البوت تحسينات في `extracted_facts` بحالة `approved=false`. يعرضها للمدير مع مصدرها وثقة الاستخلاص، ثم تُعتمد يدويًا وتُسجل في `training_revisions`. لا تتحول المحادثة تلقائيًا إلى قاعدة معرفة موثوقة، ولا يغير البوت نفسه صلاحياته.

## اختبارات دورية

يجب تشغيل اختبارات شهرية تشمل: محاولة حقن التعليمات، محاولة قراءة الأسرار، تكرار Webhook، رسائل كبيرة، محاولات دخول متكررة، طلبات غير موقعة، وانقطاع OpenAI أو Supabase. تُحفظ النتائج في سجل مراجعة ولا تُنفذ اختبارات هجومية على أنظمة لا يملكها المستخدم.

## حدود المسؤولية

هذه طبقة دفاع تطبيقية وليست بديلًا عن جدار حماية مُدار، مراقبة Render، إدارة حساب Meta، MFA، أو اختبار اختراق مستقل. لا يمكن ضمان عدم اختراق النظام، لكن يمكن تقليل الاحتمال والأثر وزمن الاستجابة.

## مراجع

[1]: https://nvlpubs.nist.gov/nistpubs/CSWP/NIST.CSWP.29.pdf "NIST Cybersecurity Framework 2.0"
[2]: https://csrc.nist.gov/pubs/sp/800/61/r3/final "NIST SP 800-61 Rev. 3 Incident Response"
[3]: https://genai.owasp.org/llmrisk/llm01-prompt-injection/ "OWASP LLM01:2025 Prompt Injection"
[4]: https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html "OWASP Secrets Management Cheat Sheet"
[5]: https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/overview "WhatsApp Webhooks - Meta for Developers"
