# قاعدة بيانات بوت أصيل

هذه الحزمة تحتوي على ملفات قاعدة البيانات الخاصة بالبوت.

## الترتيب الصحيح للتنفيذ

1. افتح مشروع Supabase.
2. افتح `SQL Editor` وأنشئ استعلامًا جديدًا.
3. الصق محتوى `supabase-schema.sql` ونفذه مرة واحدة.
4. أنشئ استعلامًا ثانيًا.
5. الصق محتوى `database-seed.sql` ونفذه بعد نجاح المخطط.
6. في Render أضف:

```text
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVER_ONLY_KEY
```

7. أعد تشغيل خدمة Render.

## محتويات قاعدة البيانات

- `users`: العملاء والمنصة وحالة التحويل لموظف.
- `chat_history`: الرسائل وسياق المحادثة وزمن الاستجابة.
- `knowledge_base`: قاعدة المعرفة المعتمدة والمصادر والمتجهات.
- `extracted_facts`: اقتراحات الحقائق غير المعتمدة للمراجعة.
- `bot_settings`: التعليمات وقاعدة المعرفة وإعدادات البوت.
- `bot_commands`: أوامر البوت مثل `/الخدمات` و`/حجز`.
- `training_revisions`: سجل تغييرات التدريب.
- `webhook_events`: أحداث Meta ومنع التكرار.
- `security_events`: الأحداث الأمنية والتنبيهات.

## الأمان

لا تستخدم `SUPABASE_SERVICE_ROLE_KEY` داخل الموقع أو المتصفح. هذا المفتاح للخادم فقط. أبقِ سياسات RLS مفعلة، ولا تضف سياسة عامة تسمح للزوار بقراءة المحادثات أو إعدادات البوت.

التطوير الذاتي لا يعتمد المعلومات مباشرة. تُحفظ الاقتراحات في `extracted_facts` مع `approved=false` حتى يراجعها المدير من لوحة الإدارة.

## المتجهات والبحث الدلالي

عمود `embedding` مجهز لـ `pgvector` بحجم 1536. يمكن إضافة Embeddings لاحقًا لكل فقرة ثم استخدام الدالة `match_knowledge`. لا تدخل نصًا غير موثوق إلى المعرفة المعتمدة دون مراجعة.
